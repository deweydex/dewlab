# What the exam builder needs.
markdown>=3.4
pyyaml>=6.0
latex2mathml>=3.77   # typesets $...$ mathematics at build time

# What developing dewmark needs on top of that.
pytest>=7.0
playwright>=1.40   # only for the dev/ browser rehearsals; needs Chromium
