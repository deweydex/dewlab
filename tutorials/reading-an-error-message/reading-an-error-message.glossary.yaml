# Generated per .claude/skills/tutorial-glossary/SKILL.md.
#
# The early half of what when-it-goes-wrong used to introduce. IndexError,
# KeyError and AttributeError stay in when-it-goes-wrong.glossary.yaml, where
# lists and dictionaries have been taught. IndentationError is new here: the
# page names it, and the old glossary did not list it.
entries:
  - term: syntax error, runtime error, logical error
    kind: concept
    definition: >
      These are the three kinds of wrong, and each is found a different
      way. A syntax error is code that is not valid Python, and Python
      catches it before anything runs. A runtime error is valid code that
      tries something impossible, and it stops the program partway. A
      logical error is code that runs to the end and gives the wrong
      answer, and only checking against an answer you already know will
      catch it.
  - term: "SyntaxError"
    python: SyntaxError
    kind: keyword
    definition: >
      Means your code is not valid Python. Python's marker often points
      just after the real problem. Modern Python points back to an
      unclosed bracket, but older versions report it where they gave up
      looking for the closing one.
  - term: "IndentationError"
    python: IndentationError
    kind: keyword
    definition: >
      Means a line is not indented the way Python expects, such as the
      line after an if that is not pushed in. It is a special kind of
      SyntaxError.
  - term: "NameError"
    python: NameError
    kind: keyword
    definition: Means you used a variable that was never created, or whose name is misspelled.
  - term: "TypeError"
    python: TypeError
    kind: keyword
    definition: Means you did something to a value that its type does not allow, such as adding a number to a string.
  - term: "ValueError"
    python: ValueError
    kind: keyword
    definition: >
      Means the type is right but the content is wrong. int("hello") gives
      int a string, which is what it wants, but not a string that means a
      number.
  - term: "ZeroDivisionError"
    python: ZeroDivisionError
    kind: keyword
    definition: Means you divided by zero. It nearly always means a value came out as zero when you expected it not to.
  - term: traceback
    kind: concept
    definition: >
      A traceback is the record of how a running program got to the place
      where it broke. Read it from the bottom: the last line says what
      went wrong, and the lines above say where. The line that failed is
      often not the line that is responsible.
