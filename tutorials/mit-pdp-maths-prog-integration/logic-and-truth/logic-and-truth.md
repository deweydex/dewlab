---
title: "Logic and Truth"
slug: logic-and-truth
module: mit-pdp-maths-prog-integration
module_title: "Programming and Maths, Integrated"
year: "2026-2027"
series: maths-and-programming
version: 2026.08.23.1
covers:
  every-possible-case:
    covers: [MIT-2.4]
  exclusive-or:
    covers: [MIT-2.4]
  de-morgans-laws:
    covers: [MIT-2.5]
  where-you-have-already-used-this:
    covers: [MIT-2.5]
  the-same-shapes-on-sets:
    covers: [MIT-2.5]
---

# Logic and Truth

**Maths for IT**

You have been writing `and`, `or` and `not` since *Making Decisions*, and you have never once seen them laid out.

That is the definition of a quiet gap. The operators are everywhere, so it looks covered -- and yet almost nobody who has been using them for a month could tell you what `not (A and B)` is equivalent to, or why a condition they wrote works but reads horribly.

This tutorial lays them out, and then shows you the two rules that let you rewrite a tangled condition into one a person can read. It comes after *Sets as Sorted Lists* on purpose: union and intersection give you the shape first, and the logic here turns out to be the same shape with different names.

## Every Possible Case

An `and` takes two true-or-false values and gives back one. There are only two possibilities for each input, so there are only four situations in total -- and four is few enough to simply list.

A list of all of them is called a **truth table**.

```python exec
id: every-possible-case-1
print("   A        B      A and B")
for a in [True, False]:
    for b in [True, False]:
        print(f"{str(a):>6} {str(b):>7} {str(a and b):>10}")
```

Four rows, generated by a loop rather than typed out. That is the point worth taking from this section: **a truth table is not a thing to memorise, it is what you get when you try every input** — and trying every input is a nested loop you could already write.

Here are the other two.

```python exec
id: every-possible-case-2
print("   A        B      A or B")
for a in [True, False]:
    for b in [True, False]:
        print(f"{str(a):>6} {str(b):>7} {str(a or b):>10}")

print()
print("   A      not A")
for a in [True, False]:
    print(f"{str(a):>6} {str(not a):>10}")
```

`or` is true when either one is, including when both are. That is the one people get wrong -- ordinary English "or" usually means one or the other but not both ("tea or coffee?"), and the logical one does not.

### Your turn

What would the table for `A and (not B)` look like? Predict the four rows, then run it to check.

```python exec
id: your-turn-1
# Your loop here.
```

## Exclusive Or

The everyday "or" that excludes both has a name: **exclusive or**, or XOR. It is true when exactly one of its inputs is true.

Python has no `xor` keyword, which turns out to be an opportunity -- you can build it three different ways from things you already have.

```python exec
id: exclusive-or-1
def xor_written_out(a, b):
    return (a or b) and not (a and b)


print("   A        B     XOR    !=     ^")
for a in [True, False]:
    for b in [True, False]:
        print(f"{str(a):>6} {str(b):>7} {str(xor_written_out(a, b)):>7}"
              f" {str(a != b):>5} {str(a ^ b):>5}")
```

Three columns, identical all the way down.

The middle one is worth a second look. For true-or-false values, "exclusive or" and "not equal to" are the *same operation* -- exactly one being true is the same as the two being different. That is not a coincidence or a trick; it is the same idea named twice by people who arrived at it from different directions.

The third, `^`, is Python's built-in bitwise XOR, which works on whole numbers bit by bit and happens to do the right thing for booleans too.

### Your turn

How might you write XOR a fourth way, using only `not` and `==`? Check it against all four rows.

```python exec
id: your-turn-2
def xor_again(a, b):
    pass


# Check it here.
```

## De Morgan's Laws

Here are the two rules, and then the proof, which is a loop.

> `not (A and B)` is the same as `(not A) or (not B)`
>
> `not (A or B)` is the same as `(not A) and (not B)`

In words: **when you push a `not` inside a bracket, the `and` becomes an `or` and the `or` becomes an `and`.**

That is easy to state and hard to believe, so do not believe it. Check it.

```python exec
id: de-morgans-laws-1
print("   A        B    not(A and B)   (not A) or (not B)")
for a in [True, False]:
    for b in [True, False]:
        left = not (a and b)
        right = (not a) or (not b)
        print(f"{str(a):>6} {str(b):>7} {str(left):>12} {str(right):>18}")
```

```python exec
id: de-morgans-laws-2
print("   A        B    not(A or B)   (not A) and (not B)")
for a in [True, False]:
    for b in [True, False]:
        left = not (a or b)
        right = (not a) and (not b)
        print(f"{str(a):>6} {str(b):>7} {str(left):>12} {str(right):>19}")
```

Identical columns, every row, both laws.

**That loop is the proof.** Not an illustration of the proof, not evidence for it -- the proof. There are exactly four cases, and the loop tried all four.

That is unusual and it is worth pausing on, because it is not how most things work. "I tested it and it worked" is normally a very weak argument, and you will meet it as a bad habit soon enough. Here it is complete, because the space of possible inputs has four things in it and we visited them all. **Exhaustive checking is a proof when the space is small enough to exhaust, and almost never otherwise.**

## Where You Have Already Used This

The reason to know De Morgan is that it turns unreadable conditions into readable ones.

Here is a condition of the kind that shows up in real code.

```python exec
id: where-you-have-already-used-this-1
def can_sit_exam(attended, submitted):
    return not (not attended or not submitted)


def can_sit_exam_readable(attended, submitted):
    return attended and submitted


print("agree everywhere:",
      all(can_sit_exam(a, s) == can_sit_exam_readable(a, s)
          for a in [True, False] for s in [True, False]))
```

The first version is three `not`s deep and takes a moment to parse. The second says what it means. They are the same function, and De Morgan is what gets you from one to the other.

Nobody writes the first version on purpose. They arrive at it by adding a condition, then negating the whole thing later, then adding another -- and the tangle grows a bit at a time. Knowing the rule is what lets you untangle it afterwards.

### Your turn

Three conditions to simplify — how would you rewrite each one? Check your versions with the same `all(...)` trick.

```python exec
id: your-turn-3
def one(a, b):
    return not (a and not b)


def two(a, b):
    return not (not a and not b)


def three(a, b, c):
    return not (a or (b and not c))


# Your simplified versions, and a check that each agrees with the original.
```

## The Same Shapes, on Sets

Now the reason this tutorial comes after *Sets as Sorted Lists*.

Everything above was about true and false. The same two laws hold for sets, with **union** where `or` was, **intersection** where `and` was, and **complement** where `not` was.

```python exec
id: the-same-shapes-on-sets-1
everyone = {1, 2, 3, 4, 5, 6, 7, 8}
a = {1, 2, 3, 4}
b = {3, 4, 5, 6}

def complement(s):
    return everyone - s


print("not (A or B):        ", complement(a | b))
print("(not A) and (not B): ", complement(a) & complement(b))
print()
print("not (A and B):       ", complement(a & b))
print("(not A) or (not B):  ", complement(a) | complement(b))
```

The same two lines, twice, with sets instead of booleans.

**De Morgan is one law wearing two costumes.** A statement being true or false, and an item being in a set or out of it, are the same question asked about different things -- and once you see that, a rule you learned in one place works in the other without being learned again.

That connection is the reason this tutorial sits where it does, and it is the most useful thing in it. It is also what [Drawing Sets](tutorial:venn-diagrams) makes into a picture: the same two laws, shaded on a diagram, proved by looking instead of by counting rows.

## Reflection

Three operators, four rows each, and two rules for moving a `not` through a bracket.

The things worth keeping.

**A truth table is a loop over every case**, not a fact to remember. If you forget what `or` does, generate it.

**Exhaustive checking is a proof only when the space is small.** Four rows is small. Almost nothing else you meet will be.

**De Morgan's Laws are for readability.** They do not make code do anything new; they make tangled conditions into ones a person can read, and you will use them mostly on code you wrote yourself a fortnight earlier.

In a few sentences, find a condition in something you have written before, and say whether De Morgan would make it easier to read.

## Where to Read More

Khan Academy. *Equivalent Compound Booleans.*
<https://www.khanacademy.org/computing/ap-computer-science-principles/programming-101/x2d2f703b37b450a3:logical-equivalence/a/equivalent-compound-booleans>.
De Morgan's Laws from the programming side, with the same two rules this
page proves by looping over four rows.
