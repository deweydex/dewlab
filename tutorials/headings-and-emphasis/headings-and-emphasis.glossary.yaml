# Generated per .claude/skills/tutorial-glossary/SKILL.md.
#
# Third tutorial of first-site. Checked against the cumulative glossary
# so far (tag, element, attribute, head, body, title, plus welcome's
# ten) -- no overlap.
entries:
  - term: heading level
    kind: concept
    definition: >
      A heading level is one of `<h1>` to `<h6>`. It tells the browser
      how important a heading is in the page's structure. `<h1>` marks a
      page's one main heading, and each level below it marks a heading
      for a smaller part of the page.
  - term: "<strong>"
    kind: keyword
    definition: >
      `<strong>` marks text as important. Browsers show it in bold,
      and the meaning stays in the code for any software that reads it.
  - term: "<em>"
    kind: keyword
    definition: >
      `<em>` marks text as emphasised, the way we would stress a word
      when we say it aloud. Browsers show it in italics, and the
      meaning stays in the code for any software that reads it.
