---
title: "Two Takes"
slug: two-takes
module: fixtures
year: "2026-2027"
series: e2e
version: 2026.09.15.1
status: live
---

<!--
A test fixture: the same tutorial in two releases, which is the only shape in
which the version picker exists at all. Two cells are in both releases and one
is only in this one, so the count of answers that carry over is a number the
tests can check rather than a phrase they can only match.

This is the default: the newest live release is what the plain URL serves.
-->

# Two Takes

Every real tutorial opens with its title as a heading.

## The second take

This is the September release.

```python exec
id: shared-one
print("one")
```

```python exec
id: shared-two
print("two")
```

```python exec
id: only-in-september
print("september")
```
